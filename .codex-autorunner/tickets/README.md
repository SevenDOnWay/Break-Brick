# Tickets

## Purpose

Pending Markdown tickets for the local Codex autorunner.

## Component Registry

- Markdown files: One implementation ticket per file.

## Communication

The runner processes exactly one Markdown file per `npm run codex:next` execution and moves it to `../working/`.
